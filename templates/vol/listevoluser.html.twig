{% extends 'template.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>
<center>
    <h1 class="titreadmin"> Vols Disponibles  </h1>
</center>

<div class="example-wrapper">
    <center>
        <table border="1" class="jointure">



            {#  <th class="jointure" >Nom de Airline</th>
    <th class="jointure">Id de vol </th>
    <th class="jointure">Nom de vol</th>
            #}



            {%for b in best%}
            {#  <tr class="jointure">


        <td class="jointure">{{b.nom}} </td>
        <td class="jointure"> {{b.id}} </td>
        <td class="jointure">{{b.nomvol}}</td>


    </tr>#}







            <div class="card">
                <div class="container">
         <h4 class="jointure" id="left"><b> {{("Nom de vol:")}} <centre>{{b.nomvol}} </centre> </b></h4>
                    <p  id="left">    {{ ("Airline :")}}       <centre>{{b.nom}}      </centre>     </p>
                    <p  id="left">    {{ ("Date de depart:")}} <centre>{{b.datedepart|date('Y-m-d')}}  </centre>      </p>
                    <p  id="left">    {{ ("Date d'arrivee:")}} <centre>{{b.datearrive|date('Y-m-d')}}  </centre>      </p>
                    <p  id="left">    {{ ("Heure de depart:")}}<centre>{{b.heuredepart|date('H:i:s')}}  </centre>    </p>
                    <p  id="left">    {{ ("Heure d'arrivé:")}} <centre>{{b.heurearrive|date('H:i:s')}}  </centre>     </p>
                    <p  id="left">    {{ ("Destination:")}}    <centre>{{b.destination}}  </centre>     </p>
                </div>
            </div>


        </table>

        {% endfor %}






    </center>
    {% endblock %}
